/** Giới hạn tin ghim mỗi hội thoại (đồng bộ với frontend `MAX_PINNED_PER_CONVERSATION`). */
export const MAX_PINNED_MESSAGES_PER_CONVERSATION = 5;

/** Tối đa số hội thoại một user được ghim lên đầu danh sách (`isPinnedToTop`). */
export const MAX_PINNED_CHATS_TO_TOP = 5;
